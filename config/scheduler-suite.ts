/** External Scheduler App — opened via in-app `/scheduler` soft handoff. */
export const SCHEDULER_APP_URL = "https://scheduler.gonline.id/";

/** Internal route that keeps Next.js navigation before leaving to Scheduler. */
export const SCHEDULER_APP_HREF = "/scheduler";

/** App icon for Scheduler nav / handoff. */
export const SCHEDULER_APP_LOGO_SRC = "/app-logo-mark.png";
